spring.application.name=student-api

# --- Configuration PostgreSQL ---
spring.datasource.url=jdbc:postgresql://localhost:5432/studentsdb
spring.datasource.username=postgres
spring.datasource.password=Ebtissem.1

# Mise à jour automatique de la structure
spring.jpa.hibernate.ddl-auto=update

# Affiche le SQL dans la console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Désactivation de la console H2 (inutile avec Postgres)
spring.h2.console.enabled=false